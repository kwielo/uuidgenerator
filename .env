# Default environment variables. Override locally in .env.local (never committed).
APP_ENV=prod
APP_DEBUG=0
# Override APP_SECRET in .env.local or via a real environment variable in production.
APP_SECRET=change_me_in_env_local
